"""
Runtime game API: the stable contract games are written against.

Exposes the public surface (enums, the ``RuntimeGame`` protocol, and re-exports
of the metadata/handler/option types) that game plugins import to declare and
implement their behavior.
"""
